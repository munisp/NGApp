FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /ussd-gateway .

FROM alpine:3.19
RUN apk --no-cache add ca-certificates tzdata
COPY --from=builder /ussd-gateway /ussd-gateway
EXPOSE 8093
CMD ["/ussd-gateway"]
