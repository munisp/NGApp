[package]
name = "remittance-graph"
version = "0.1.0"
edition = "2021"
description = "FalkorDB graph engine for remittance corridor analysis and fraud detection"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
