[package]
name = "pbac-engine"
version = "1.0.0"
edition = "2021"
description = "Policy-Based Access Control engine for payment switch platform"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
name = "pbac_engine"
path = "src/lib.rs"
