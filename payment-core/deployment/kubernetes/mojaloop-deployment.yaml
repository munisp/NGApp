apiVersion: v1
kind: Service
metadata:
  name: mojaloop-api-adapter
  namespace: payment-switch
spec:
  ports:
  - port: 3000
    targetPort: 3000
    name: http
  selector:
    app: mojaloop
    component: api-adapter
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mojaloop-api-adapter
  namespace: payment-switch
spec:
  replicas: 3
  selector:
    matchLabels:
      app: mojaloop
      component: api-adapter
  template:
    metadata:
      labels:
        app: mojaloop
        component: api-adapter
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "mojaloop-api-adapter"
        dapr.io/app-port: "3000"
        dapr.io/config: "dapr-config"
    spec:
      containers:
      - name: api-adapter
        image: mojaloop/ml-api-adapter:v14.0.0
        ports:
        - containerPort: 3000
        env:
        - name: MLAPI_ENDPOINT_SOURCE_URL
          value: "http://mojaloop-api-adapter:3000"
        - name: MLAPI_ENDPOINT_HEALTH_URL
          value: "http://mojaloop-api-adapter:3000/health"
        - name: MLAPI_KAFKA_HOST
          value: "kafka"
        - name: MLAPI_KAFKA_PORT
          value: "9092"
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: mojaloop-central-ledger
  namespace: payment-switch
spec:
  ports:
  - port: 3001
    targetPort: 3001
    name: http
  selector:
    app: mojaloop
    component: central-ledger
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mojaloop-central-ledger
  namespace: payment-switch
spec:
  replicas: 3
  selector:
    matchLabels:
      app: mojaloop
      component: central-ledger
  template:
    metadata:
      labels:
        app: mojaloop
        component: central-ledger
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "mojaloop-central-ledger"
        dapr.io/app-port: "3001"
        dapr.io/config: "dapr-config"
    spec:
      containers:
      - name: central-ledger
        image: mojaloop/central-ledger:v17.0.0
        ports:
        - containerPort: 3001
        env:
        - name: CLEDG_DATABASE_URI
          value: "postgresql://central_ledger:password@mojaloop-postgres:5432/central_ledger"
        - name: CLEDG_KAFKA_HOST
          value: "kafka"
        - name: CLEDG_KAFKA_PORT
          value: "9092"
        resources:
          requests:
            memory: "1Gi"
            cpu: "500m"
          limits:
            memory: "2Gi"
            cpu: "1000m"
---
apiVersion: v1
kind: Service
metadata:
  name: mojaloop-account-lookup
  namespace: payment-switch
spec:
  ports:
  - port: 4002
    targetPort: 4002
    name: http
  selector:
    app: mojaloop
    component: account-lookup
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mojaloop-account-lookup
  namespace: payment-switch
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mojaloop
      component: account-lookup
  template:
    metadata:
      labels:
        app: mojaloop
        component: account-lookup
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "mojaloop-account-lookup"
        dapr.io/app-port: "4002"
        dapr.io/config: "dapr-config"
    spec:
      containers:
      - name: account-lookup
        image: mojaloop/account-lookup-service:v14.0.0
        ports:
        - containerPort: 4002
        env:
        - name: ALS_DATABASE_URI
          value: "postgresql://account_lookup:password@mojaloop-postgres:5432/account_lookup"
        - name: ALS_KAFKA_HOST
          value: "kafka"
        - name: ALS_KAFKA_PORT
          value: "9092"
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: mojaloop-quoting-service
  namespace: payment-switch
spec:
  ports:
  - port: 3002
    targetPort: 3002
    name: http
  selector:
    app: mojaloop
    component: quoting-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mojaloop-quoting-service
  namespace: payment-switch
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mojaloop
      component: quoting-service
  template:
    metadata:
      labels:
        app: mojaloop
        component: quoting-service
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "mojaloop-quoting-service"
        dapr.io/app-port: "3002"
        dapr.io/config: "dapr-config"
    spec:
      containers:
      - name: quoting-service
        image: mojaloop/quoting-service:v15.0.0
        ports:
        - containerPort: 3002
        env:
        - name: QS_DATABASE_URI
          value: "postgresql://quoting_service:password@mojaloop-postgres:5432/quoting_service"
        - name: QS_KAFKA_HOST
          value: "kafka"
        - name: QS_KAFKA_PORT
          value: "9092"
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: mojaloop-postgres
  namespace: payment-switch
spec:
  ports:
  - port: 5432
    targetPort: 5432
  selector:
    app: mojaloop-postgres
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mojaloop-postgres
  namespace: payment-switch
spec:
  serviceName: mojaloop-postgres
  replicas: 1
  selector:
    matchLabels:
      app: mojaloop-postgres
  template:
    metadata:
      labels:
        app: mojaloop-postgres
    spec:
      containers:
      - name: postgres
        image: postgres:15-alpine
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_PASSWORD
          value: "rootpassword"
        - name: POSTGRES_DB
          value: "central_ledger"
        - name: POSTGRES_USER
          value: "mojaloop"
        volumeMounts:
        - name: postgres-data
          mountPath: /var/lib/postgresql/data
        resources:
          requests:
            memory: "2Gi"
            cpu: "1000m"
          limits:
            memory: "4Gi"
            cpu: "2000m"
  volumeClaimTemplates:
  - metadata:
      name: postgres-data
    spec:
      accessModes: [ "ReadWriteOnce" ]
      storageClassName: "standard"
      resources:
        requests:
          storage: 50Gi
