# Auto-generated K8s manifest for langchain-agent-go
apiVersion: v1
kind: Namespace
metadata:
  name: 54bank
  labels:
    istio-injection: enabled
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: langchain-agent-go
  namespace: 54bank
  labels:
    app: langchain-agent-go
    language: go
    tier: microservice
spec:
  replicas: 3
  selector:
    matchLabels:
      app: langchain-agent-go
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: langchain-agent-go
        language: go
        tier: microservice
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8512"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: langchain-agent-go
      terminationGracePeriodSeconds: 30
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: langchain-agent-go
          image: ghcr.io/54bank/langchain-agent-go:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8512
              name: http
              protocol: TCP
          env:
            - name: PORT
              value: "8512"
            - name: SERVICE_NAME
              value: "langchain-agent-go"
            - name: ENV
              valueFrom:
                configMapKeyRef:
                  name: 54bank-config
                  key: ENV
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: 54bank-db
                  key: url
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: 54bank-redis
                  key: url
            - name: NEO4J_BOLT_URL
              valueFrom:
                secretKeyRef:
                  name: 54bank-neo4j
                  key: bolt_url
                  optional: true
            - name: FALKORDB_URL
              valueFrom:
                secretKeyRef:
                  name: 54bank-falkordb
                  key: url
                  optional: true
            - name: QDRANT_URL
              valueFrom:
                secretKeyRef:
                  name: 54bank-qdrant
                  key: url
                  optional: true
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: 54bank-openai
                  key: api_key
                  optional: true
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /livez
              port: 8512
            initialDelaySeconds: 10
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8512
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /healthz
              port: 8512
            initialDelaySeconds: 3
            periodSeconds: 5
            failureThreshold: 10
---
apiVersion: v1
kind: Service
metadata:
  name: langchain-agent-go-svc
  namespace: 54bank
  labels:
    app: langchain-agent-go
spec:
  selector:
    app: langchain-agent-go
  ports:
    - port: 8512
      targetPort: 8512
      protocol: TCP
      name: http
  type: ClusterIP
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: langchain-agent-go
  namespace: 54bank
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: langchain-agent-go-hpa
  namespace: 54bank
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: langchain-agent-go
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 75
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: langchain-agent-go-pdb
  namespace: 54bank
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: langchain-agent-go
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: langchain-agent-go-netpol
  namespace: 54bank
spec:
  podSelector:
    matchLabels:
      app: langchain-agent-go
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: 54bank
      ports:
        - protocol: TCP
          port: 8512
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              name: 54bank
