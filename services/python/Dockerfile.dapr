FROM python:3.12-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir fastapi uvicorn httpx pydantic
COPY dapr/ dapr/
EXPOSE 8092
CMD ["python", "-m", "uvicorn", "dapr.app:app", "--host", "0.0.0.0", "--port", "8092"]
