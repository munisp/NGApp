# Thanos object store config (#50) — long-term metrics to MinIO/S3
type: S3
config:
  bucket: payment-switch-metrics
  endpoint: minio:9000
  access_key: minioadmin
  secret_key: minioadmin
  insecure: true
  http_config:
    idle_conn_timeout: 90s
    response_header_timeout: 2m
