import 'package:flutter/material.dart';

class BatchTransferScreen extends StatelessWidget {
  const BatchTransferScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Batch Transfer')),
      body: const Center(child: Text('Batch Transfer - Full Implementation')),
    );
  }
}
