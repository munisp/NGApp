FROM rust:1.75-alpine AS builder
RUN apk add --no-cache musl-dev
WORKDIR /app
COPY services/rust/mdm-engine/ .
RUN cargo build --release

FROM alpine:3.19
COPY --from=builder /app/target/release/mdm-engine /usr/local/bin/
EXPOSE 8087
CMD ["mdm-engine"]
