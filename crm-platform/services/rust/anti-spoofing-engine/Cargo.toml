[package]
name = "anti-spoofing-engine"
version = "1.0.0"
edition = "2021"
description = "Anti-spoofing engine for facial liveness detection — detects printed photos, screen replay, masks, deepfakes"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
