apiVersion: apps/v1
kind: Deployment
metadata:
  name: mdm-engine
  namespace: crm
  labels:
    app: mdm-engine
    platform: crm
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mdm-engine
  template:
    metadata:
      labels:
        app: mdm-engine
        platform: crm
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8106"
    spec:
      containers:
        - name: mdm-engine
          image: crm-platform/mdm-engine:latest
          ports:
            - containerPort: 8106
          env:
            - name: PORT
              value: "8106"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: crm-secrets
                  key: database-url
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: crm-secrets
                  key: redis-url
            - name: KAFKA_BROKERS
              value: kafka.crm.svc:9092
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              cpu: 400m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /health
              port: 8106
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: 8106
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: mdm-engine-service
  namespace: crm
spec:
  selector:
    app: mdm-engine
  ports:
    - port: 8106
      targetPort: 8106
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: mdm-engine-hpa
  namespace: crm
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: mdm-engine
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
