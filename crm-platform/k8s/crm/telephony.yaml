apiVersion: apps/v1
kind: Deployment
metadata:
  name: telephony
  namespace: crm
  labels:
    app: telephony
    platform: crm
spec:
  replicas: 2
  selector:
    matchLabels:
      app: telephony
  template:
    metadata:
      labels:
        app: telephony
        platform: crm
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8099"
    spec:
      containers:
        - name: telephony
          image: crm-platform/telephony:latest
          ports:
            - containerPort: 8099
          env:
            - name: PORT
              value: "8099"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: crm-secrets
                  key: database-url
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: crm-secrets
                  key: redis-url
            - name: KAFKA_BROKERS
              value: kafka.crm.svc:9092
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 200m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /health
              port: 8099
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: 8099
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: telephony-service
  namespace: crm
spec:
  selector:
    app: telephony
  ports:
    - port: 8099
      targetPort: 8099
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: telephony-hpa
  namespace: crm
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: telephony
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
