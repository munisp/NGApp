apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent-governance
  namespace: crm
  labels:
    app: agent-governance
    tier: crm
spec:
  replicas: 2
  selector:
    matchLabels:
      app: agent-governance
  template:
    metadata:
      labels:
        app: agent-governance
    spec:
      containers:
        - name: agent-governance
          image: crm/agent-governance:latest
          ports:
            - containerPort: 8093
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: crm-db-credentials
                  key: url
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8093
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8093
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: agent-governance
  namespace: crm
spec:
  selector:
    app: agent-governance
  ports:
    - port: 8093
      targetPort: 8093
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: agent-governance
  namespace: crm
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: agent-governance
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
