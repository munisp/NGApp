apiVersion: apps/v1
kind: Deployment
metadata:
  name: falkordb-graph
  namespace: crm
  labels:
    app: falkordb-graph
    platform: crm
spec:
  replicas: 2
  selector:
    matchLabels:
      app: falkordb-graph
  template:
    metadata:
      labels:
        app: falkordb-graph
        platform: crm
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8091"
    spec:
      containers:
        - name: falkordb-graph
          image: crm-platform/falkordb-graph:latest
          ports:
            - containerPort: 8091
          env:
            - name: PORT
              value: "8091"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: crm-secrets
                  key: database-url
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: crm-secrets
                  key: redis-url
            - name: KAFKA_BROKERS
              value: kafka.crm.svc:9092
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              cpu: 400m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /health
              port: 8091
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: 8091
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: falkordb-graph-service
  namespace: crm
spec:
  selector:
    app: falkordb-graph
  ports:
    - port: 8091
      targetPort: 8091
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: falkordb-graph-hpa
  namespace: crm
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: falkordb-graph
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
