apiVersion: apps/v1
kind: Deployment
metadata:
  name: campaign-analytics
  namespace: crm
  labels:
    app: campaign-analytics
    platform: crm
spec:
  replicas: 2
  selector:
    matchLabels:
      app: campaign-analytics
  template:
    metadata:
      labels:
        app: campaign-analytics
        platform: crm
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8087"
    spec:
      containers:
        - name: campaign-analytics
          image: crm-platform/campaign-analytics:latest
          ports:
            - containerPort: 8087
          env:
            - name: PORT
              value: "8087"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: crm-secrets
                  key: database-url
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: crm-secrets
                  key: redis-url
            - name: KAFKA_BROKERS
              value: kafka.crm.svc:9092
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              cpu: 400m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /health
              port: 8087
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: 8087
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: campaign-analytics-service
  namespace: crm
spec:
  selector:
    app: campaign-analytics
  ports:
    - port: 8087
      targetPort: 8087
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: campaign-analytics-hpa
  namespace: crm
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: campaign-analytics
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
