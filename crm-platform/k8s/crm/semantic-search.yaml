apiVersion: apps/v1
kind: Deployment
metadata:
  name: semantic-search
  namespace: crm
  labels:
    app: semantic-search
    tier: crm
spec:
  replicas: 2
  selector:
    matchLabels:
      app: semantic-search
  template:
    metadata:
      labels:
        app: semantic-search
    spec:
      containers:
        - name: semantic-search
          image: crm/semantic-search:latest
          ports:
            - containerPort: 8094
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: crm-db-credentials
                  key: url
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8094
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8094
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: semantic-search
  namespace: crm
spec:
  selector:
    app: semantic-search
  ports:
    - port: 8094
      targetPort: 8094
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: semantic-search
  namespace: crm
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: semantic-search
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
