apiVersion: apps/v1
kind: Deployment
metadata:
  name: art-security
  namespace: crm
  labels:
    app: art-security
    platform: crm
spec:
  replicas: 1
  selector:
    matchLabels:
      app: art-security
  template:
    metadata:
      labels:
        app: art-security
        platform: crm
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8095"
    spec:
      containers:
        - name: art-security
          image: crm-platform/art-security:latest
          ports:
            - containerPort: 8095
          env:
            - name: PORT
              value: "8095"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: crm-secrets
                  key: database-url
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: crm-secrets
                  key: redis-url
            - name: KAFKA_BROKERS
              value: kafka.crm.svc:9092
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 200m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /health
              port: 8095
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: 8095
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: art-security-service
  namespace: crm
spec:
  selector:
    app: art-security
  ports:
    - port: 8095
      targetPort: 8095
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: art-security-hpa
  namespace: crm
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: art-security
  minReplicas: 1
  maxReplicas: 3
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
