apiVersion: apps/v1
kind: Deployment
metadata:
  name: acquisition-engine
  namespace: crm
  labels:
    app: acquisition-engine
    platform: crm
spec:
  replicas: 2
  selector:
    matchLabels:
      app: acquisition-engine
  template:
    metadata:
      labels:
        app: acquisition-engine
        platform: crm
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8089"
    spec:
      containers:
        - name: acquisition-engine
          image: crm-platform/acquisition-engine:latest
          ports:
            - containerPort: 8089
          env:
            - name: PORT
              value: "8089"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: crm-secrets
                  key: database-url
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: crm-secrets
                  key: redis-url
            - name: KAFKA_BROKERS
              value: kafka.crm.svc:9092
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 200m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /health
              port: 8089
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: 8089
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: acquisition-engine-service
  namespace: crm
spec:
  selector:
    app: acquisition-engine
  ports:
    - port: 8089
      targetPort: 8089
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: acquisition-engine-hpa
  namespace: crm
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: acquisition-engine
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
