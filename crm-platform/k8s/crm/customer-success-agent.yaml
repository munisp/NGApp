apiVersion: apps/v1
kind: Deployment
metadata:
  name: customer-success-agent
  namespace: crm
  labels:
    app: customer-success-agent
    tier: crm
spec:
  replicas: 2
  selector:
    matchLabels:
      app: customer-success-agent
  template:
    metadata:
      labels:
        app: customer-success-agent
    spec:
      containers:
        - name: customer-success-agent
          image: crm/customer-success-agent:latest
          ports:
            - containerPort: 8092
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: crm-db-credentials
                  key: url
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8092
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8092
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: customer-success-agent
  namespace: crm
spec:
  selector:
    app: customer-success-agent
  ports:
    - port: 8092
      targetPort: 8092
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: customer-success-agent
  namespace: crm
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: customer-success-agent
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
